services:
  eero-dashboard:
    build: .
    ports:
      - "8420:8420"
    volumes:
      - eero-data:/app/data
    environment:
      - SPEED_HISTORY_DAYS=365
    restart: unless-stopped

volumes:
  eero-data:
